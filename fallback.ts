/**
This is to be the fallback endpoint that calculates individual assets lazily and adds them to the storage.

As it's all about static assets (md, html, json, js, css, etc) we're not concerned about other methods like POST
*/
export const GET = () => {
  // get which deployment it's about
  // get proper prompts to be made (workflow)
  // execute workflow and respond with proper result
};
