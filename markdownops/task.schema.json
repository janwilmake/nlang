{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "metadata": {
      "description": "User-provided metadata stored with the run. Keys and values must be strings with a maximum length of 16 and 512 characters respectively.",
      "type": "object",
      "additionalProperties": { "type": "string", "maxLength": 512 }
    },
    "input_schema_url": {
      "type": "string",
      "description": "JSON schema or text description of expected input to the task",
      "format": "uri"
    },
    "output_schema_url": {
      "type": "string",
      "description": "URL leading to a JSON schema or text fully describing the desired output from the task. Descriptions of output fields will determine the form and content of the response. ",
      "format": "uri"
    },
    "processor": {
      "type": "string",
      "enum": [
        "lite",
        "base",
        "core",
        "pro",
        "ultra",
        "ultra2x",
        "ultra4x",
        "ultra8x"
      ]
    }
  }
}
